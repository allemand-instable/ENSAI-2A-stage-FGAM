% ~ proof

\newcommand{\insertproof}[1]{%
	\ifnum\value{proofrenderbool}=1%
		{%
		\input{#1}%
		}%
	\else
		{%
			%
		}%
	\fi
}

% ~ MINTED

\newcommand{\insertcode}[1]{%
	\ifnum\value{code}=1%
		{%
		\input{#1}%
		}%
	\else
		{%
			%
		}%
	\fi
}
